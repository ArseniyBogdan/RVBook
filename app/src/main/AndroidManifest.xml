<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recyclerviewtest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecyclerViewTest">
        <activity
            android:name=".FirstSettings.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".FirstSettings.SettingsActivity"
            android:exported="true" />
        <activity
            android:name=".FirstSettings.HelperActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GetStudentMarks.GetStudentMarksActivity" />
        <activity android:name=".GetMarks.GetMarksActivity" />
        <activity
            android:name=".AddDateMarks.AddMarksActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".Students.StudentsActivity" />
        <activity android:name=".Button_Activity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"/>
    </application>

</manifest>